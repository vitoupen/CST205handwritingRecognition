import Tkinter

window = Tkinter.Tk()

unusedText = "Hello CST 205!"

def closeWin(): 
    window.destroy()

def printUnusedText():
    print unusedText

# when button clicked, invokes closeWin function
B = Tkinter.Button(window, text="Press me", command=closeWin)

# packer is one of Tk's geometry-management mechanisms
# pack() method defaults to side = "top"
B.pack()

window.mainloop()

